const { DataTypes } = require('sequelize');
const sequelize = require('../utils/connection');

const Articulos_menores = sequelize.define('articulos_menores', {
    
});

module.exports = Articulos_menores